[project]
name = "catalog-validation"
version = "0.1.0"
description = "Validate Sogrim catalog JSON files against schema and source PDFs"
requires-python = ">=3.11"
dependencies = [
    "pymupdf>=1.24.0",
    "requests>=2.31.0",
]
