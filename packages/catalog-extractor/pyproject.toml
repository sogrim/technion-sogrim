[project]
name = "catalog-extractor"
version = "0.1.0"
description = "Extract Technion PDF course catalogs into structured JSON"
requires-python = ">=3.11"
dependencies = [
    "pymupdf>=1.24.0",
    "requests>=2.31.0",
]
